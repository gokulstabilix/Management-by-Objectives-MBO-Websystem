# ── Frontend Environment Variables ──
# Vite requires env vars to be prefixed with VITE_ to be exposed to the client.

# Backend API base URL (include the /api path)
VITE_API_URL=http://localhost:5000/api
